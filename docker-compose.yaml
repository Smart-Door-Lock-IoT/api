name: api-smart-door-lock
services:
  database:
    image: postgres:17.5-alpine3.22
    ports:
      - "15432:5432"
    environment:
      - POSTGRES_USER=root
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_DB=smart_door_lock
      - TZ=Asia/Jakarta
      - PGTZ=Asia/Jakarta